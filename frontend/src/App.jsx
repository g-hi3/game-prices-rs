import styles from "./App.module.css";

const App = () => {
    return (
        <div class={styles.App}>
            <header class={styles.header}>
                <p>Hello World</p>
            </header>
        </div>
    );
};

export default App;
